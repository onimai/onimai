import { component$ } from "@builder.io/qwik";

export default component$(() => {
  return (
    <div>
      <h2>Button</h2>
      <p>
        <button>Click me</button>
      </p>
    </div>
  );
});
