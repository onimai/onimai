{
  "name": "onimaiui",
  "version": "0.1.0",
  "license": "MIT",
  "main": "./index.js",
  "scripts": {
    "build": "yarn build:postcss && yarn build:prejss",
    "build:postcss": "postcss src/index.css --dir dist",
    "build:prejss": "prejss-cli dist/*.css"
  },
  "dependencies": {
    "tailwindcss": "^3.2.4"
  },
  "devDependencies": {
    "postcss": "^8.4.21",
    "postcss-cli": "^10.1.0",
    "postcss-import": "^15.1.0",
    "prejss-cli": "^0.3.3"
  }
}
