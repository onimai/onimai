{
  "private": true,
  "name": "onimai",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "yarn workspace onimai run build && yarn workspace @onimai/qwik run build && yarn workspace onimai-docs run build"
  }
}
